# 安全策略（SECURITY）

这个仓库是一个**只读**的日志分析 Runtime：它不执行任何处置动作。
但它会**读你上传的日志** —— 所以这里最需要守住的不是"有没有漏洞"，
而是"**敏感原文会不会被带出去**"。

---

## 一、怎么报

| 类型 | 走哪条路 |
|---|---|
| **敏感类**：能导致原文外泄的缺陷（脱敏漏了、原文进了磁盘或数据库、`.env` 能被读出来、密钥进了日志） | **不要开公开 issue。**走仓库 **Security → Report a vulnerability**（GitHub 私密漏洞报告） |
| **一般类**：崩溃、解析错、文档与行为不符、性能问题 | 直接开 issue；模板会提示要带哪几样（见 `CONTRIBUTING.md` 第 5 节） |

**两种都不要粘贴真实密钥、真实日志原文或 `.env` 内容。**
需要给证据时：用自己造的假数据复现，或只描述"哪一类字段没被替换"。

---

## 二、这个项目对"泄露"的定义（三条硬约束，来自 `AGENTS.md`）

1. **脱敏在落盘前**：上传内容在**内存里**就完成脱敏，写盘、入库、进分析 Context、
   发给模型的都只有脱敏后的文本（`app/services/upload_service.py` 的 ①→⑥ 顺序）。
2. **密钥只进 `.env`**：`.env` 被 `.gitignore` / `.dockerignore` 挡住；
   单测 `test_no_real_key_anywhere_in_tracked_files` 会扫**全部已跟踪文件**
   （它只在本地配了 Key 时真正执行；CI 上没有 Key，按设计跳过）。
3. **失败不静默**：配置缺失、模型不可用、拿不到证据，一律显式降级并留痕，
   不允许产出"看起来正常"的结果。

---

## 三、已知的、按设计存在的行为（**不算漏洞**）

- **日志内容会发给模型供应商**（`MODEL_PROVIDER_BASE_URL` 指向的那家），
  但发的是**脱敏后**的片段。不接受把内容发给第三方就别配模型：
  链路会降级成纯规则报告，并在 Run 上写明原因。
- **Web access 平文日志会被整批计为坏行**（`logs/nginx_plain.log` 就是负样本）：
  本领域只认 syslog / log4j / Apache error / 逐行 JSON，边界写在 `README.md` 第 3 节。
- **V1 是单机自用**：默认只绑 `127.0.0.1`，没有多用户权限体系，
  也没有为"公网暴露"做过加固。放到公网前请自行评估。
- **脱敏是规则式的，不是万无一失的**：它针对 IP、邮箱、常见凭据形态等模式，
  误伤与漏伤都存在量化基线（见 `logs/README.md`）。别把未脱敏的真实生产日志
  直接喂给它 —— 先确认你的敏感字段在覆盖范围内。

---

## 四、支持范围

只维护 `main` 上的 V1；没有 LTS 分支，也没有安全补丁承诺。
