# 阶段验收表（真机口径）

生成时间：2026-09-24 20:49:45　|　测试：735 用例 / 失败 0 / 错误 0 / 跳过 3　|　静态接线：入口点 36，可达符号 488/620

口径：**每条验收 = 测试证据 + 是否在真实路径上被调用**。三种证据见 `.dsh/stage_acceptance.py` 文件头。

| # | 验收条目 | 测试证据 | 真机接线（静态） | 真机实测（运行时） | 结论 |
|---|---|---|---|---|---|
| 01-1 | `docker compose up` 起 PostgreSQL + Redis | test_env_connectivity.py 5 | —（无锚点） | compose 五服务在跑，健康检查经真实 HTTP 通过 | ✅ 达标 |
| 01-2 | FastAPI 启动并返回健康检查 | test_env_connectivity.py 5 | 接入 2/2 | /healthz 200、/healthz/deps 200（pg+redis ok=True） | ✅ 达标 |
| 01-3 | Celery worker 能启动并消费一个测试任务 | （全量 pytest，见 13-1） | 接入 2/2 | Run 由 logagent-worker/直跑 worker 消费（同一 trace 内 _execute 执行 2 次） | ✅ 达标 |
| 01-4 | `pytest` 可运行（先放一个冒烟测试） | （全量 pytest，见 13-1） | —（无锚点） | 735 用例，失败 0，错误 0，跳过 3（跳过的是需 RUN_LIVE_MODEL_TESTS=1 的付费用例） | ✅ 达标 |
| 01-5 | 配置全部来自环境变量，代码里无密钥 | test_config.py 5, test_delivery_config.py 37 | 接入 1/1 | 扫 app/ 下 sk- 形态字面量：0 处；配置项逐个确认被读取（settings_usage_audit 无死配置） | ✅ 达标 |
| 02-1 | 迁移可执行可回滚 | （全量 pytest，见 13-1） | —（无锚点） | alembic upgrade head 在真机执行成功（migrate 容器 Exited(0)）；downgrade 没有自动化用例，本轮未实测回滚 | ⚠️ 待实测 |
| 02-2 | 能创建 Project / DataSource | test_api_pg.py 49 | 接入 1/1 | 真实 HTTP 建项目 6943、数据源 4929，落库可查 | ✅ 达标 |
| 02-3 | 能插入普通事件与指标事件并按 project 查回 | test_models_pg.py 11, test_upload_pipeline.py 19 | 接入 1/1 | 上传 cpu_anomaly 指标日志后 metric 事件落库 10 条（并按 project 查回） | ✅ 达标 |
| 02-4 | 事件可归入 EventGroup、再归并为 Incident 并查回 | test_models_pg.py 11 | 接入 0/2（create_group、create_incident 未接入） | 真机 event_groups=0、incidents=0（全库 177 条 Run 也全为 0）：归并只发生在内存里，create_group/create_incident 无生产调用方 | ❌ 未达标 |
| 02-5 | `model_calls` 可追加记录 | test_models_pg.py 11 | 接入 1/1 | model_calls 追加 1 条：[('L3', 'deepseek-flash')] | ✅ 达标 |
| 03-1 | 能注册并加载 computer_monitoring | test_domain_contract.py 27 | 接入 3/3 | worker 内 build_default_registry 执行 2 次、ComputerMonitoringDomain 被加载 | ✅ 达标 |
| 03-2 | 不修改 Runtime 代码即可被替换 | test_domain_contract.py 27 | 接入 1/1 | —（本轮未采集） | ✅ 达标 |
| 03-3 | 对样例行能产出标准事件、指标事件 | test_parser_computer_monitoring.py 30, test_upload_pipeline.py 19 | 接入 2/2 | 真实语料 logs/Mac_2k.log 解析入库 2000 条事件 | ✅ 达标 |
| 03-4 | 命中异常时能取到对应 runbook | test_analyzers_and_loaders.py 51 | 接入 2/2 | 真机 Run 里带 runbook 的结论 0 条；重放同一条真机事件链得到：候选异常 1 条、attach_runbooks 挂上 0 条（命中词 'crash' 没有对应 runbook —— rb_crash_001 要求 segfault/panic，rb_oom_001 要求 oom，而 analyzer 命中的是 'crash'） | ❌ 未达标 |
| 04-1 | 上传 TXT / JSONL 能解析为事件入库，响应含解析统计 | test_upload_pipeline.py 19 | 接入 1/1 | 真实 JSONL 1000 行解析 1000、坏行 0；真实 syslog 语料 2000 行全解析 | ✅ 达标 |
| 04-2 | 含邮箱 / 密钥的内容在落盘文件和数据库中都已被替换 | test_upload_pipeline.py 19 | 接入 1/1 | 注入 canary 后：落盘文件无原文（email=False、secret=False）、含 12 个 [EMAIL_] + 2 个 [SECRET_] 标记、库内含原文事件 0 条 | ✅ 达标 |
| 04-3 | 坏行 / 无法识别的时间戳被计数并明确报告，不静默丢弃 | test_upload_pipeline.py 19 | —（无锚点） | 负样本 nginx_plain.log：1000/1000 计为坏行并附样本（有意为之） | ✅ 达标 |
| 04-4 | 未带 data_source_id 的上传能自动建 / 复用 DataSource | test_upload_pipeline.py 19 | 接入 1/1 | 首次上传自动建源（created=True），二次同名格式上传复用同一源 (created=False, id=4932) | ✅ 达标 |
| 04-5 | 超过大小上限的上传被拒绝并给出明确提示 | test_upload_pipeline.py 19 | 接入 1/1 | 11600000 字节（上限 10 MiB）→ HTTP 413 | ✅ 达标 |
| 05-1 | 三个工具结果正确、边界行为明确 | test_tools_data_ops.py 40 | 接入 1/3（event_filter、time_window 未接入） | 本轮真机各工具执行次数 {'stats_calculator': 2, 'event_filter': 0, 'time_window': 0}：只有 stats_calculator 在链路上，event_filter / time_window 一次都没跑 | ❌ 未达标 |
| 05-2 | 注册器能按名取用 | test_tools_registry.py 21 | 接入 0/2（get_tool_registry、call 未接入） | get_tool_registry 真机执行 0 次；链路直接 import stats_calculator，注册表在生产路径上没有调用方 | ❌ 未达标 |
| 05-3 | 执行结果可记录到 Run | test_tool_usage_recorded.py 5 | 接入 0/1（append_tool_usage 未接入） | 真机 Run.tool_usage = None（append_tool_usage 无生产调用方，恒为空） | ❌ 未达标 |
| 06-1 | 至少接通一个真实供应商并返回结构化结果 | test_gateway_router.py 43 | 接入 2/2 | 真实 DeepSeek 调用成功（gateway.execute 追踪命中 2 次），结构化结果入库 | ✅ 达标 |
| 06-2 | 切换型号只改环境变量、不改业务代码 | test_gateway_router.py 43 | 接入 1/1 | Run 记录的等级→型号：[('L3', 'deepseek-flash')]；业务代码里无型号字面量（单测钉死），换型号只需改 .env | ✅ 达标 |
| 06-3 | token 与成本被记录 | test_gateway_router.py 43 | 接入 1/1 | tokens 6366/4324、cost ¥0.023662 均落库 | ✅ 达标 |
| 07-1 | 预算不足拒绝创建 | test_policy_cost.py 56, test_cost_control_pg.py 5 | 接入 2/2 | 项目预算 0.001 → HTTP 402：项目剩余预算 ¥0.0010 不足以覆盖预计成本 ¥0.0176 | ✅ 达标 |
| 07-2 | 人为设置极小预算能触发 partial_success 且页面明确标注「不完整」 | test_cost_control_pg.py 5 | 接入 2/2 | 本轮未造出 partial_success 的真机 Run（需策略覆盖把单次上限压到一次调用之下）；单测与集成测试有证据 | ⚠️ 待实测 |
| 07-3 | 成本被正确累加 | test_cost_control_pg.py 5 | 接入 1/1 | Project.budget_used=0.0237 与 Run.cost_actual=0.023662 一致（Numeric(12,4) 的显示精度内；Post-check 真机生效） | ✅ 达标 |
| 07-4 | 无法构造出无限调用的 Run | test_policy_cost.py 56 | 接入 1/1 | 本次 Run 模型调用 1 次（策略上限 max_model_calls=5），mid-check 在每次调用前生效 | ✅ 达标 |
| 07-5 | 月度预算不足 → 拒绝创建（计划第 644 行；跨月边界） | test_policy_cost.py 56, test_run_lifecycle_pg.py 35 | 接入 2/2 | 端到端真机核验：真实分析花掉 ¥0.016743 → 库里「本月已花」=¥0.016743 → 再发起被 402 拦下（本月已花 ¥0.0167，月度预算 ¥0.0200 剩余 ¥0.0033 不足以覆盖预计成本 ¥0.0088）；跨月边界：上月花满 → 放行（202）；逻辑侧：本月已花满 → 402 | ✅ 达标 |
| 07-6 | 峰谷价（MODEL_PEAK_PRICE_MULTIPLIER）在成本上生效 | test_policy_cost.py 56 | 接入 1/1 | 实际计费侧按峰谷生效（高峰 ×2.0），但 Pre-check 估算侧与峰谷无关：估算 ¥0.0176 vs 同 token 在高峰的实际计费 ¥0.032（高峰时低估 1.82 倍） | ❌ 未达标 |
| 08-1 | 创建 Run 立即返回 `run_id + queued` | test_run_lifecycle_pg.py 35 | 接入 1/1 | 创建 Run 返回 202，耗时 0.275s，状态 queued | ✅ 达标 |
| 08-2 | 手动 kill Worker 后僵尸 Run 被回收 | test_run_lifecycle_pg.py 35, test_e2e_isolation_pg.py 16 | 接入 1/1 | 真机实测：分析进行中冻结 Worker → Run 3378 被 beat 容器上（maintenance 专用队列）的回收任务收成 timeout（心跳丢失，104.3s 未更新）；分析 Worker 全程未参与回收 —— 救火队不在消防站里 | ✅ 达标 |
| 08-3 | 重复提交不产生重复 Run | test_run_lifecycle_pg.py 35 | 接入 1/1 | 同一输入重复提交 → run_id 相同=True（reused=True，第二次 202） | ✅ 达标 |
| 08-4 | 取消能在下一个检查点生效 | test_run_lifecycle_pg.py 35, test_reliability.py 75 | 接入 2/2 | 真机实测：分析进行到 8s 调取消（HTTP 200，cancel_requested 落库=True），状态 ['running', 'cancelled'] → 在下一个检查点收成 cancelled；根因（Worker 全程持未提交事务、取消写入被行锁挡住）已随 checkpoint 提交修掉 | ✅ 达标 |
| 08-5 | 模型不可用时能降级到 L0 报告 | test_run_lifecycle_pg.py 35, test_reliability.py 75 | 接入 2/2 | 本轮两条真机 Run 都调通了模型；L0 降级路径只有单测/集成测试证据 | ⚠️ 待实测 |
| 08-6 | 各类错误分类正确 | test_reliability.py 75 | 接入 1/1 | classify_error 本轮真机执行 0 次（两条 Run 均成功，未触发失败分类路径） | ⚠️ 待实测 |
| 09-1 | 正常日志走 L0 | test_analysis_pipeline.py 83 | 接入 1/1 | 空数据源的真机 Run 走 L0（model_attempts=0、cost=0），等级判定在链路内执行 | ✅ 达标 |
| 09-2 | 少量错误走 L1 | test_analysis_pipeline.py 83 | —（无锚点） | 本轮未取到 L1 的真机样本（真实语料直接 L2→L3 升级，空源走 L0） | ⚠️ 待实测 |
| 09-3 | 多错误 / 高危异常走到相应等级 | test_analysis_pipeline.py 83 | 接入 1/1 | 真实语料与崩溃样本均升到 L3（Run 内 attempts=1，context_tokens=6948） | ✅ 达标 |
| 09-4 | 事件能分组并归并为事故、历史相似事故被注入 Context | test_analysis_pipeline.py 83 | 接入 2/2 | 内存里确实做了分组与归并（trace 命中 cluster_events/merge_into_incidents），但结果不落库（event_groups/incidents 全库为 0），且 historical_incidents 在 app/ 内无任何赋值处 → 「历史相似事故注入 Context」在真机上恒为空 | ❌ 未达标 |
| 09-5 | Context 不超预算 | test_analysis_pipeline.py 83 | 接入 1/1 | L3 context_tokens=6948 ≤ 预算 8000（L1/L2 分别为 2000/4000） | ✅ 达标 |
| 09-6 | 构造「模型返回假 event_id」的用例能拦截并重试 / 降级 | test_analysis_pipeline.py 83 | 接入 2/2 | 真机没有喂假 event_id（会伪造模型输出）；单测有 3 条针对性用例 | ⚠️ 待实测 |
| 09-7 | 每个 fact 都有有效证据 | test_analysis_pipeline.py 83, test_pipeline_persistence_pg.py 14 | 接入 1/1 | 真机 fact 无证据 0 条、证据引用越界 0 条（12 条结论 12 条证据，逐条可核对） | ✅ 达标 |
| 10-1 | 每个端点鉴权与参数校验生效 | test_api_pg.py 49 | 接入 2/2 | 无 token → 401；缺字段 → 422；越权建项目 → 422 | ✅ 达标 |
| 10-2 | 分析端点立即返回、不阻塞 | test_api_pg.py 49 | 接入 1/1 | 创建 Run 返回 202，耗时 0.275s，状态 queued | ✅ 达标 |
| 10-3 | 接口文档 `/docs` 可访问 | test_api_pg.py 49 | —（无锚点） | /docs → 200 | ✅ 达标 |
| 10-4 | 跨 Project 访问被拒绝 | test_api_pg.py 49, test_e2e_isolation_pg.py 16 | 接入 1/1 | 他人读本项目数据源 → 404（Run 同样 404） | ✅ 达标 |
| 11-1 | 从上传到看报告全程不碰命令行 | test_web_ui_contract.py 5, test_web_ui_pg.py 19 | 接入 1/1 | 页面 ['/login', '/projects', '/knowledge/7137'] 均 200 且渲染 HTML；页面 JS 调用的接口与真实路由由单测逐条对齐 | ✅ 达标 |
| 11-2 | fact 的证据可点击核对 | test_web_ui_pg.py 19 | 接入 1/1 | 证据条数 12，引用的 event_id 全部落在本次事件集合内 | ✅ 达标 |
| 11-3 | runbook 可见 | test_web_ui_pg.py 19 | 接入 1/1 | 真机 Run 里带 runbook 的结论 0 条；重放同一条真机事件链得到：候选异常 1 条、attach_runbooks 挂上 0 条（命中词 'crash' 没有对应 runbook —— rb_crash_001 要求 segfault/panic，rb_oom_001 要求 oom，而 analyzer 命中的是 'crash'） | ❌ 未达标 |
| 11-4 | 不完整 / 失败结果有显著提示与重试入口 | test_web_ui_pg.py 19 | 接入 1/1 | 重试端点存在且对不可重试状态给明确原因（{'detail': "Run 3115 当前状态 completed 不允许重新分析；只有 ['failed', 'partial_success', 'ti）；但本轮没有造出 failed/partial_success 的 Run，重试成功路径未在真机验证 | ⚠️ 待实测 |
| 12-1 | 给定任一 Run 能完整复述「系统做了什么、花了多少、为何得到这个结论」 | test_observability_pg.py 21 | 接入 1/1 | Run 详情复述含状态/花费/阶段/结论：Run #3023（项目 6943，trace 13e7167600194d14b763154919ab7774） 状态 completed…（trace_id=13e7167600194d14b763154919ab7774 贯穿 web 与 worker） | ✅ 达标 |
| 13-1 | 单测通过 | （全量 pytest，见 13-1） | —（无锚点） | 735 用例，失败 0，错误 0，跳过 3（跳过的是需 RUN_LIVE_MODEL_TESTS=1 的付费用例） | ✅ 达标 |
| 13-2 | Golden Set 五个场景全部符合预期 | test_golden_set.py 17 | —（无锚点） | Golden Set 离线 18/18 断言通过，¥0.0 | ✅ 达标 |
| 13-3 | E2E 跑通 | （全量 pytest，见 13-1） | —（无锚点） | 对真跑起来的全栈走 16 项端到端检查：16/16 | ✅ 达标 |
| 13-4 | 隔离攻击用例全部失败于系统防线 | test_e2e_isolation_pg.py 16 | —（无锚点） | —（本轮未采集） | ✅ 达标 |
| 14-1 | 全新环境 clone 后，按 README 能在本机一键起全栈并完成一次真实分析 | （全量 pytest，见 13-1） | —（无锚点） | compose 五服务 + 真实分析跑通（16/16）；镜像源码哈希与工作区逐文件一致（3 处抽样）；'全新环境 clone' 未在本机复现（本机已有 .env 与数据卷） | ⚠️ 待实测 |

## 汇总

- ✅ 达标：42
- ⚠️ 待实测（测试与接线通过、本轮无运行时证据）：8
- ❌ 未达标：8

### 未达标清单（真机不生效）

- 02-4 事件可归入 EventGroup、再归并为 Incident 并查回 —— 真机接线缺口：create_group、create_incident
- 03-4 命中异常时能取到对应 runbook —— 真机 Run 里带 runbook 的结论 0 条；重放同一条真机事件链得到：候选异常 1 条、attach_runbooks 挂上 0 条（命中词 'crash' 没有对应 runbook —— rb_crash_001 要求 segfault/panic，rb_oom_001 要求 oom，而 analyzer 命中的是 'crash'）
- 05-1 三个工具结果正确、边界行为明确 —— 真机接线缺口：event_filter、time_window
- 05-2 注册器能按名取用 —— 真机接线缺口：get_tool_registry、call
- 05-3 执行结果可记录到 Run —— 真机接线缺口：append_tool_usage
- 07-6 峰谷价（MODEL_PEAK_PRICE_MULTIPLIER）在成本上生效 —— 实际计费侧按峰谷生效（高峰 ×2.0），但 Pre-check 估算侧与峰谷无关：估算 ¥0.0176 vs 同 token 在高峰的实际计费 ¥0.032（高峰时低估 1.82 倍）
- 09-4 事件能分组并归并为事故、历史相似事故被注入 Context —— 内存里确实做了分组与归并（trace 命中 cluster_events/merge_into_incidents），但结果不落库（event_groups/incidents 全库为 0），且 historical_incidents 在 app/ 内无任何赋值处 → 「历史相似事故注入 Context」在真机上恒为空
- 11-3 runbook 可见 —— 真机 Run 里带 runbook 的结论 0 条；重放同一条真机事件链得到：候选异常 1 条、attach_runbooks 挂上 0 条（命中词 'crash' 没有对应 runbook —— rb_crash_001 要求 segfault/panic，rb_oom_001 要求 oom，而 analyzer 命中的是 'crash'）

## 怎么复现这张表

```bash
# ① 真机跑一遍（会产生模型费用；产生的都是真实证据）
python .dsh/live_path_trace.py web 8001        # 终端 A：带函数级追踪的 Web
$env:REDIS_URL='redis://127.0.0.1:6379/1'; python .dsh/live_path_trace.py worker   # 终端 B：带追踪的 Worker
python .dsh/real_path_evidence.py             # 两次真实分析 + 落库/落盘事实 + 函数追踪
python .dsh/real_path_evidence.py --contract  # 接口契约（健康检查/鉴权/隔离/取消/重试/幂等）
python .dsh/real_path_evidence.py --uploads   # 只上传：JSONL、负样本、指标、超限、复用
python .dsh/real_path_evidence.py --zombie-start   # 起 Run 后强杀 Worker，再 --zombie-check
python .dsh/real_path_evidence.py --cancel-test    # 分析中调取消，看它停不停
python .dsh/real_path_evidence.py --sweep     # 容器全栈 E2E + Golden Set
python .dsh/budget_gate_check.py              # 成本闸门：四项 + 月度预算跨月 + 峰谷价
python .dsh/runbook_probe.py <project_id>     # runbook 为什么挂不上
python .dsh/cancel_stale_probe.py <run_id>    # 取消为什么慢一步（行锁）

# ② 出表
python .dsh/stage_acceptance.py
```

成本口径：本轮三次真实分析合计约 **¥0.07**（Run 3023/3024/3028 与容器 E2E 一次），
其余检查（闸门、契约、上传、超限、追踪）零模型费用。
