# 开工前 6 条文档不一致：判定与修订说明

> 对应 WorkBuddy 待办：③ 「开工前敲定的」6 条。
> 这些是**同一份计划内部打架 / 未定义**的地方，会在对应阶段真正卡住执行。
> 下面的判定已同步进 `Log_Intelligence_Agent_V1_个人全栈工程编码计划.md（最新版）.md`。
> 凡是我替你做主的，都给了「为什么」与「可后悔点」；涉及只有你能定的（真实日志、Key）不在本文。

---

## 1. 知识审核缺 API（阶段 10）→ 已补端点

**问题**：阶段 11 要做「列出候选 / 确认 / 编辑 / 丢弃 / 标记 false_positive」，但阶段 10 端点清单里一个 knowledge 相关端点都没有。

**判定**：补齐 6 个端点（已写入阶段 10）。前端阶段 11 的「待确认知识区」不再需要绕过 API 直读文件。

```text
GET    /api/projects/{id}/knowledge/candidates        # 列出 staging 候选
GET    /api/projects/{id}/knowledge/confirmed         # 列出已生效知识
POST   /api/knowledge/candidates/{candidate_id}/confirm        # 确认 → 移入 confirmed
PATCH  /api/knowledge/candidates/{candidate_id}                # 编辑后再确认
POST   /api/knowledge/candidates/{candidate_id}/reject         # 丢弃
POST   /api/knowledge/candidates/{candidate_id}/false-positive # 标记误报 → confirmed/false_positives
```

**为什么**：知识闭环（8.1）是 V1 垂直能力增长引擎之一，必须有干净的运行时入口；让前端直接读写 YAML 会把文件格式耦进 UI，且无法做 evidence 校验与鉴权。
**可后悔点**：端点粒度可合并（如 confirm/reject/false-positive 合成一个 `PATCH status`），V1 从简即可。

---

## 2. knowledge 是「代码资产」还是「运行时数据」→ 定为：seed 是代码资产，confirmed/staging 是运行时数据

**问题**：8.1 说知识 YAML「随 domain 目录包一起版本化、进 Git」；阶段 11 又要用户在网页上确认、写进 `confirmed/`。运行时由 Web 往代码目录写数据——Docker 里该目录是镜像内容，不挂卷重启即丢，挂卷又和 Git 版本化打架。

**判定（拆分，不混）**：

| 类别 | 位置 | 性质 | 进 Git？ | 进镜像？ | 挂卷？ |
|---|---|---|---|---|---|
| **seed 知识**（你手写的初始 confirmed） | `app/domains/<domain>/knowledge/seed/*.yaml` | **代码资产** | ✅ | ✅（只读） | ❌ |
| **运行时 confirmed**（人工确认后生效） | `${DATA_DIR}/knowledge/<domain>/confirmed/*.yaml` | **运行时数据** | ❌（gitignore） | ❌ | ✅ |
| **运行时 staging**（模型候选） | `${DATA_DIR}/knowledge/<domain>/staging/candidates.yaml` | **运行时数据** | ❌ | ❌ | ✅ |

- 加载顺序：先读 `seed/`，再叠加 `${DATA_DIR}/knowledge/.../confirmed/`（运行时覆盖 seed）。
- 写入只发生在人工确认时，**只写数据卷，不写代码目录**。
- 版本化：运行时知识库**单独备份 / 或单独一个 data 仓库**版本化，不与代码仓库混。

**为什么**：这是「代码进镜像、数据进卷」的标准边界。混在一起会让镜像不可复现、重启丢数据、Git 脏。
**可后悔点**：V1 知识量小，若你嫌两层麻烦，可只保留运行时数据卷、把 seed 合并进去；但**「不往代码目录写运行时数据」这条不能破**。

---

## 3. Run 没有「阶段进度」字段（阶段 02 / 11 / 12）→ 加两个字段

**问题**：阶段 11 状态页要显示阶段进度、阶段 12 要可追踪阶段，但 `AgentRun` 只有一个 `status`。

**判定**：`AgentRun` 显式增加两列（不再只靠 `run_metadata` 塞）：

```text
current_phase   VARCHAR   # 当前阶段，如 "parse" / "group" / "analyze" / "model" / "validate"
phase_history   JSONB     # [{"phase":"parse","status":"done","at":...}, ...]
```

- `status` 管 7 态生命周期；`current_phase` / `phase_history` 管「跑到哪一步」——两者正交，不要混。
- `run_metadata` 继续管 `stop_reason` / `completed_phases` / `skipped_phases`（计划 07 节已有）。

**为什么**：状态页轮询需要细粒度进度，塞进 `run_metadata` 会失去可索引性、且语义混杂。
**可后悔点**：若嫌两列重，可只留 `phase_history`；但**必须显式写进表结构**，不能默认「塞 metadata 就行」。

---

## 4. `Event.group_id` 与 `EventGroup.event_ids` 双写、真源未定 → 真源 = `Event.group_id`，删除 `EventGroup.event_ids`

**问题**：两者互为反向索引的双写；1 万事件量级下 jsonb 数组膨胀，且有双写一致性风险。没写谁是真源。

**判定**：

- **`Event.group_id`（外键，可空）是唯一真源**：一个事件属于哪个分组。
- **删除 `EventGroup.event_ids`**：分组查成员事件时按 `WHERE group_id = ?` 查，不再维护反向数组。
- 保留 `EventGroup.event_count`，作为分组时一次性写好的**缓存计数**（不随事件逐条更新）。
- 同理处理 Incident：**`Incident.group_ids`（多对多，V1 无中间表）是真源**；**删除 `Incident.event_ids`**（可由成员分组的事件派生）。

**为什么**：单向外键 + 按需查询，避免双写与数组膨胀；1 万量级下 `WHERE group_id` 走索引完全够。
**可后悔点**：若将来频繁「按分组列事件」成为热点，再加反向缓存字段也可以，但那时应有真源已定的前提。

---

## 5. Incident 归并的「同根因」怎么判（阶段 09）→ 定义 V1 启发式（非因果）

**问题**：V1 明确不做跨文件 baseline、不做因果分析，那「同根因」拿什么判？

**判定（V1 用「同源 + 同时间窗 + 同异常签名」近似，明确写清这是代理指标，不是因果推断）**：

同一 `source_id` 下的多个 `EventGroup`，**同时满足**以下条件才归并为一个 `Incident`：

```text
① 同 source_id；
② 时间窗重叠，或间隔 ≤ MERGE_WINDOW（默认 10 分钟）；
③ 命中同一「异常签名」：相同 analyzer 类型，或同一模板簇家族（template cluster）；
④ 可选：severity 同档（high/medium/low 之间不跨档合并，除非 ③ 完全一致）。
```

- 归并是**确定性代码**（红线 1：确定性计算不调用 LLM）。
- 可复用已有 Incident：新分析的组若满足上述条件且与历史 Incident 时间窗连续，则并入该 Incident。
- **在文档与报告里明确措辞**：这是「同源同窗同签名」的近似分组，**不是根因判定**；根因仍由模型给出、且必须标 `inference`/`possibility` 并挂证据。

**为什么**：给阶段 09 一个可编码、可测试、可迭代的起点，同时不违反「不做因果分析」的边界。
**可后悔点**：阈值（10 分钟、签名口径）靠 Golden Set 调；这是最该先用真实日志验证的一块。

---

## 6. 两处小的 + `.env` 缺 `MODEL_PRICE_*`

**(a) 模板聚类算法 → V1 手写「正则变量替换」，不引 Drain3**

- 判定：V1 用**手写规则**（把数字 / IP / hex / 时间戳 / 路径等可变部分替换为占位符，得到模板，再按模板聚合）。
- 理由：零额外依赖、行为可解释、易写单测；Drain3 留作 V1.5 的「再评估信号」（当手写规则在真实日志上聚类质量不够时）。
- 「可后悔点」：若真实日志模板差异极大、手写覆盖不住，再引入库。

**(b) 鉴权 → JWT（Bearer）+ Web 侧存 httpOnly Cookie**

- 判定：用 **JWT**。API 走 `Authorization: Bearer <token>`；极简网页把 token 存 `httpOnly` Cookie。
- 理由：计划的前端是「Jinja2 + 原生 JS、无构建」，JWT 无状态、跨 API/页面统一，比 session 少一层服务端状态；用 `pyjwt`，依赖轻。
- 自用单账号，不做角色系统（计划 1 节已定）。

**(c) `.env` 缺 `MODEL_PRICE_*` → 已补**

- `MODEL_{L1,L2,L3}_PRICE_{INPUT,OUTPUT}_PER_1M`（阶段 01 骨架已补，本方案沿用）。详见《模型与Key配置方案.md》。
- 同时提醒：示例里的 `MONTHLY_BUDGET=10` / `DEFAULT_RUN_MAX_COST=0.30` 若不声明货币会与「建议先用美元」打架——V1 直接统一为**元**即可。
