# --- 密钥 / 本地配置 ---
.env
.env.local
.env.*
!.env.example
*.pem
*.key

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- 沙箱下的工具缓存（本机 %LOCALAPPDATA% / D: 不可写，被重定向进项目）---
# uv：全局 UV_CACHE_DIR=D:\dev-cache\uv-cache 在沙箱下不可写，需覆盖为项目内目录
.uv-cache/
# npm：npx 下载 MCP server 时 %LOCALAPPDATA%\npm-cache 报 EPERM，需覆盖为项目内目录
.npm-cache/
# 测试探针（系统临时目录在沙箱下不可枚举，探针库落在工作区内）
.pytest-tmp/
# pytest 在缓存目录不可写时留下的临时目录（沙箱下删不掉，别让它进版本库）
pytest-cache-files-*/
# alembic 执行后留下的字节码目录
migrations/__pycache__/

# --- 评测 / 验收脚本的**产出**（脚本本身在 .dsh/ 里，要进版本库）---
# 这些是每次跑都会变的中间产物：junit 报告、评测日志、Golden 上传的脱敏副本
.dsh/junit*/
.dsh/*.xml
.dsh/*.txt
.dsh/golden-uploads/
# 函数级追踪原始数据（摘要已并入 runtime_evidence.json，原始文件太大且可重跑）
.dsh/trace_*.json
# 僵尸实验的临时记录：里面有本次会话的 JWT，绝不进版本库
.dsh/zombie_test.json

# --- 运行时数据 ---
# uploads/ 存的是脱敏后的日志，属运行数据，不进版本库
uploads/*
!uploads/.gitkeep
# 知识库运行时数据（confirmed/staging）走数据卷，不进代码仓库
data/
# 全新 clone 交付验证会在工作区里放一份**完整副本**，里面还有按 README 复制进去的 .env：
# 脚本正常跑完会删掉它，一旦中断留下，这份副本绝不能进版本库（否则等于泄一次密钥）
.fresh-clone/

# --- 工具 ---
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- 已被取代的「来源快照」（不是遗漏，见下）---
# 这两处放的是 skills 与 AGENTS.md 的**原始快照**：
#   - 生效副本在 `.dsh/skills/`（stage-verify 已按真机口径重写，快照停在旧版本）
#   - 生效的 `AGENTS.md` 在**项目根**（DSH 每次会话自动加载它；子目录里的那份不会）
# 同一份东西两处存在必然分叉，所以只留一份真源：
#   `mcp-内置skills/` 与 `tools-skills-prompt…/dsh-skills/` 留在本地供溯源，不进版本库；
#   同目录里**项目自己写的**文档（DSH 提示词清单、调研报告）仍然入库。
mcp-内置skills/
tools-skills-prompt与选型和ide移植/AGENTS.md
tools-skills-prompt与选型和ide移植/dsh-skills/
