# 真机核验报告（2026-09-24）

> 这份报告是《[阶段验收表-真机口径.md](阶段验收表-真机口径.md)》的说明部分。
> 结论一句话：**按新口径重跑 60 条验收，60 条达标、0 条待实测、0 条真机未达标。**
> （首轮 39 / 8 / 11 → 第一批修复 42 / 8 / 8 → 第二批 52 / 8 / 0 → 补测 56 / 4 / 0 → 全部补齐 60 / 0 / 0。）
> 上一轮"14 个阶段全 ✅"是在"测试全绿 = 达标"的旧口径下得出的，本轮证明那个口径会漏。

---

## 0. 本轮修复记录（修完即真机复验，不是"改完就算"）

### 第一批：三条最疼的（月度预算取数 / 取消 / 僵尸回收）

| # | 缺陷 | 修法 | 真机复验证据 |
|---|---|---|---|
| 1 | `started_at` 从不写入 → 月度预算取数恒为 0（3.4） | `apply_status` 首次置 `running` 时写 `started_at` | Run 3139/3148 均有 `started_at`；全库"花过钱且 started_at 非空"的 Run 从 0 条变成有值 |
| 2 | `MONTHLY_BUDGET` 环境变量从未被读（策略里硬编码 10.0，3.4 的孪生缺陷） | `policy_from_settings()` 把 `monthly_budget` 也搬进策略 | 把月度预算设成 ¥0.02，真实分析花掉 ¥0.0167 → 再发起被 **402** 拦下："本月已花 ¥0.0167，月度预算 ¥0.0200 剩余 ¥0.0033 不足以覆盖预计成本 ¥0.0088" |
| 3 | 取消不生效：Worker 长事务持行锁，取消写入被挡到任务结束（3.7） | ① `RunExecutor` 增加 checkpoint，状态/心跳写完即提交；② `is_cancel_requested` 改查标量列，绕开身份映射 | 分析进行到 8s 调取消 → 状态 `running → cancelled`（Run 3140）；`LockNotAvailable` 复现脚本随之失效 |
| 4 | 僵尸 Run 无人回收：beat 无 schedule（3.6） | 加 `beat_schedule` + `reap_zombie_runs` 任务，**走专用 `maintenance` 队列**，由 beat 容器内嵌 worker 消费 | 分析中冻结 Worker 容器 → Run 3143/3378 被维护队列收成 `timeout`（心跳丢失），**分析 Worker 全程停摆也没影响回收** |
| 5 | （复验时逼出来的）被回收成 `timeout` 的 Run 被"迟到的 Worker"复活成 `completed`，且回收原因被旧快照抹掉 | `apply_status`/`_record_crash`/`merge_run_metadata` 一律查库取当前值 | 冻结→回收→解冻：Run 3378 保持 `timeout`，`stop_reason=timeout`、"心跳丢失"的 note 与本次实际花费/结论并存 |

第 4 条的**第一版是错的**，而且是真机复验当场发现的：回收任务最初发到默认队列
（与分析任务同一个），于是分析 Worker 一被冻结，回收任务就跟着排队没人消费 ——
而"分析 Worker 死了"正是它唯一要处理的场景。**救火队不能住在消防站里**：
现在维护任务走独立队列，由 beat 容器内嵌的 worker 消费，与 web/worker 完全解耦。

第 5 条更是只有真机才暴露得出来：单测里"回收"和"迟到完成"分属两个用例、
互不干扰；真机上两者会撞在同一个 Run 上。它连带说明一件事：
**跨进程的"终态不可再改"必须查库判断**，读会话里缓存的对象是不成立的。

### 第二批：剩下的 8 条（分组/事故、runbook、峰谷价、工具链、知识闭环）

| # | 缺陷 | 修法 | 真机复验证据 |
|---|---|---|---|
| 6 | **分组与事故从不落库**（3.1）：177 条 Run 跑完，`event_groups` 与 `incidents` 两张表恒为 0 | 新增 `persist_grouping()`：建/复用 EventGroup（唯一键 `(project, source, group_key)`）、建/合并 Incident、回填 `Event.group_id` / `incident_id` | Run 3566：`event_groups=1848`、`incidents=593`、**2000/2000** 个事件回填了归属；第二次分析 `groups_created=0`（复用）、`incidents_reused=6` |
| 7 | **历史事故注入恒为空**（3.8）：`historical_incidents` 在 app/ 内无任何赋值处 | Worker 构造 `PipelineInput` 时按项目取最近 5 条事故（`recent_for_context`，source_id 由成员分组派生、signature 由标题前缀承载） | 同项目第二次分析：`run_context_sources.historical_incidents = 5` |
| 8 | **runbook 从不挂上**（3.2）：analyzer 命中 `crash`，runbook 只认 segfault/panic/core dumped | 关键词表对齐（`rb_crash_001` 加 `crash`）＋ `find_runbook_for(matched_pattern=…)`：命中可能来自进程名，消息里没有那个词 | 真机两条 Run：结论带 runbook **15/15** 与 **6/6** |
| 9 | **峰谷价只在计费侧生效**（3.5）：估算侧不传 `peak`，高峰低估 1.82 倍 | `estimate_run_cost(..., peak=)`；`CostController(peak_now=)` 由组合根按 `is_peak_time` 装配（与计费侧同一判断） | 高峰口径下估算 ¥0.0352 = 计费 ¥0.032 × 1.1 安全边际；装配出的控制器 `peak_now` 与 `is_peak_time(now)` 一致 |
| 10 | **工具注册表没接进管线**（3.3）：`get_tool_registry` 真机执行 0 次，`Run.tool_usage` 恒为 null | 统计与样本筛选改经 `registry.call("stats_calculator"/"event_filter", …)`；`PipelineResult.tool_runs` 由 Worker 写进 `Run.tool_usage` | 真机 Run：`tool_usage = event_filter×1（2000 条输入）、stats_calculator×1（3ms）`。`time_window` 仍已注册无调用方，**如实标注、不为了用而用** |
| 11 | **知识闭环断在两处**（新增条目 09-8 / 13-5）：候选从不写 staging；confirmed 知识从不加载 | ① Worker 分析后写 `staging/candidates.yaml`；② 分析前加载 `domain.knowledge()["confirmed"]` | 真机：分析后经知识审核接口读到候选 4/6 条；确认一条后**下一次分析加载 confirmed 知识 1 条**（Run 3571） |
| 12 | （复验时逼出来的）模型自造 `kind: new_pattern` / `rule_gap` → 加载器拒载**整份文件** → 确认过的知识也读不回来 | ① schema 里 `kind` 加 enum；② `_validate_candidates` 把 kind 归一化到协议层白名单；③ 加载器区分**代码资产严格、运行时数据逐条容错**（坏条目跳过并指名告警） | 复验：`confirmed_knowledge` 从 0 变成 1；坏条目被跳过、同文件里的好条目照常生效 |

第 12 条同样只有真机才撞得出：单测里候选的 kind 都写得很规范，
而真模型会自造词 —— 自造词一旦落进数据卷，就让**整份**知识文件失效，
闭环断在最后一步（"确认过了，但下次分析读不到"）。

### 第三批：把剩下的"待实测"逐条补上证据

| # | 场景 | 做法（都是真机真实路径） | 结果 |
|---|---|---|---|
| 13 | 迁移可执行可回滚 | 临时库上跑 `alembic upgrade head` → `downgrade base` → `upgrade head` | 10 张表 → 业务表全清（只剩 `alembic_version`）→ 表数复原；主库未受影响 |
| 14 | 模型不可用降级 L0 | 把 Key 换成无效值，跑一次真实分析 | Run **`partial_success`** + `stop_reason=model_unavailable` + 纯规则报告、¥0 |
| 15 | 错误分类 | 同上，读 `run_metadata.attempts` 的 `error_kind` | 分类器在真实路径上跑过（不可重试的错误不被无谓重试） |
| 16 | 失败/不完整结果的重试入口 | 对上面那条 `partial_success` 走「重新分析」 | HTTP 202，新 Run 的 `parent_run_id` 指回原 Run |

第 14 条的第一版也是**错的**，同样是真机当场打脸：Key 无效时 Run 被收成
`completed`、零结论零花费 —— 看起来像"分析跑完了没发现异常"，
而计划第 724 行要的是 `partial_success` + `model_unavailable`。
根因：管线内部把模型失败吞成了纯规则报告，而"这份报告是失败换来的"这条信息
只挂在**结果对象**上，执行器的降级判定看不到它（它只看自己的 outcome）。
现在管线会打 `model_unavailable` 标记，执行器据此把这一级算作失败、
交给降级链继续尝试，最终由兜底路径产出报告并按 partial 收尾。

第 16 条又牵出一处：**不填时间范围发起的 Run 根本无法重试** ——
快照里 `time_range` 是 `null`（原样照抄了请求），而 API 实际用的是
`_epoch()..now` 兜底窗口，重试据此解析必然 422。现在创建时就把**生效的**
窗口写进快照，并补了回归用例。

> 剩下的 4 条「待实测」都写清了缺什么，不装懂：
> `07-2` 极小预算触发 partial_success、`09-2` L1 真机样本、
> `09-6` 假 event_id、`14-1`"全新环境 clone"。
> **这四条已在第四批补齐**，见下。

### 第四批：把最后 4 条「待实测」也补成真机证据

| # | 场景 | 做法（都是真机真实路径） | 结果 |
|---|---|---|---|
| 17 | 假 event_id 拦截（09-6） | 写一个**本地桩供应商**（OpenAI 兼容），返回一条引用不存在 `event_id` 的 `fact`；请求仍走真实的 Gateway → Router → 链路 | 证据被拦下 1 条、链路带反馈重试、库里 0 条结论、引用假 id 的证据 0 条、无证据的 fact 0 条 |
| 18 | 极小预算触发 partial_success（07-2） | 同一个桩 + 单次上限 ¥0.019：估算是 ¥0.0176（预检放行），桩上报的用量让第一次调用实花 ¥0.020 | Run 收成 **`partial_success` + `stop_reason=budget_exceeded`**（"已花成本 ¥0.0200 达到单次上限 ¥0.0190"）——mid-check 在**第二次调用前**生效 |
| 19 | 少量错误走 L1（09-2） | 造一份"6 行、2 条 ERROR、无异常命中"的真实格式日志，走容器栈真模型 | Run 3807 判到 **L1**、context 518 tokens（L1 预算 2000）、¥0.005、产出 6 条结论 |
| 20 | 全新环境 clone（14-1） | `git clone` → 按 README 放 `.env` → 原栈 `down` → clone 里 `docker compose up -d --build` → clone 里跑 16 项 E2E → 原栈起回来 | clone 1209 个文件且**不含 `.env`**；一键起全栈健康；E2E **16/16**（含一次真实分析 ¥0.015）；原栈恢复、数据卷保留 |

第 17/18 条的做法值得说明：**桩供应商是"可控的反常输入"，不是"绕过真实依赖"**。
真模型不会配合你造假 event_id，而这一类输入恰恰是红线 3 必须拦住的；
把供应商那一端换成桩之后，Gateway、Router、证据闸门、成本闸门全都还是真的 ——
这也是唯一能在零费用下反复验证"异常输入"的办法。

第 19 条补的是一个**判级样本缺口**：此前真机样本只有 L0（空源）与 L2→L3
（有 high 异常），没有正好落在 L1 的；现在有了，而且顺带验证了
"context 不超预算"在 L1 档位上也成立。

第 20 条是唯一一条**必须停掉正在跑的全栈**才能验的：compose 里端口与容器名都是
写死的（与"只绑 127.0.0.1"的定位一致），所以真验一遍就意味着 down → 起 clone 栈 →
验完 down → 把原栈起回来、确认数据卷还在。脚本 `.dsh/fresh_clone_check.py` 把这条
路径固定下来了，谁都能重跑。

> 另注：这两批一共把 `event_groups` / `incidents` / `tool_usage` / `staging` 四处
> "写好了但没人接"补齐；`time_window` 是唯一**有意保留**没有生产调用方的工具，
> 理由写在表里（复杂度评估用间隔聚类，与按固定窗口切分不是一回事）。

---

## 1. 为什么要换口径

上一轮给 14 个阶段打的 ✅ 全部基于测试证据（693 用例全绿）。而阶段 07 的教训恰恰是
**测试全绿 ≠ 真机生效**：`CostController` 的三道闸门当时代码齐全、单测全绿，
真机上一次都没被调用过 —— 因为没有任何真实路径构造过它。

所以本轮把每条验收拆成**必须同时具备的证据**：

| 证据 | 工具 | 回答的问题 |
|---|---|---|
| 测试证据 | `.dsh/junit/all.xml`（pytest 逐用例计数） | 逻辑对不对 |
| 真机接线（静态） | `.dsh/wiring_graph.py`：从真实入口点（FastAPI 路由 / Celery 任务 / 启动期语句）做**调用图可达性** | 从真机入口**能不能**走到它 |
| 真机实测（运行时） | `.dsh/runtime_evidence.json`：真跑一遍的落库、落盘、函数级追踪事实 | 这一次运行里它**到底跑了没有** |

判定规则：锚点有未接入 → ❌；运行时证据不达标 → ❌；只有测试+接线、本轮没跑到 → ⚠️；
**任何一条都不许只凭"测试全绿"给 ✅**。

### 为什么需要新工具，而不是继续用旧的

- `dead_wiring_audit.py`（上一轮写的）只扫**顶层**函数/类，方法级的接线缺口它结构上就看不见 ——
  本轮 6 处缺口里有 4 处（`append_tool_usage`、`reclaim_zombies`、`write_candidates`、
  `get_tool_registry`）都是方法或用 `__all__` 引用过的符号，它一条都没报。
- `wiring_graph.py` 把**方法**纳入、并从**入口点**出发找调用链，才把这一类扫干净。
- 静态可达只能证明"接得上"。要证明"这一次真的跑了"，只有运行时追踪：
  `.dsh/live_path_trace.py` 给 Web 与 Worker 两侧装函数级 tracer，
  跑完得到"哪些函数被执行了、各多少次"。

---

## 2. 本轮真机实测做了什么

首轮（发现问题的那些运行）：

| 场景 | 方式 | 关键结果 |
|---|---|---|
| 真实语料分析 | `logs/Mac_2k.log`（真实 macOS syslog，尾部注入脱敏 canary）→ 真实 HTTP → Celery → 模型 | Run 3023：2000 事件、12 结论 12 证据、L2→L3、¥0.0237 |
| 崩溃样本分析 | `tests/datasets/crash/system.log` | Run 3024：16 事件、7 结论、L3、¥0.0296 |
| 取消实验 | 分析进行到一半调取消 | Run 3028：`cancel_requested` 落库但仍跑完、¥0.0324（根因：行锁） |
| 僵尸实验 | 分析中强杀 Worker，等 10 分钟 | Run 3029：停在 `queued`，无人回收 |
| 成本闸门 | 真 HTTP + 手工造的历史 Run | 本月已花满 → 402；上月花满 → 放行；峰谷价只在计费侧生效 |
| 容器全栈 E2E | `docker compose` 五服务 + `final_verify.cjs` | 16/16，真实分析一次（Run 3114，¥0.0138） |
| Golden Set | `.dsh/golden_eval.py`（离线） | 18/18 断言，5 个场景，¥0 |
| 全量测试 | `pytest` | 失败 0、错误 0、跳过 3（付费用例） |

修复后的复验（第二轮及以后）：

| 场景 | 方式 | 关键结果 |
|---|---|---|
| 月度预算端到端 | 月度预算调到 ¥0.02，跑一次真实分析再发起第二次 | 真实花掉 ¥0.0167 → 库里「本月已花」同步 → **402 拦下** |
| 取消 | 分析进行到 8s 调取消 | `running → cancelled`（Run 3140） |
| 僵尸回收 | 冻结 Worker 容器（`docker compose pause`），维护队列回收 | Run 3143/3378 → `timeout`，**分析 Worker 全程停摆** |
| 复活缺陷 | 冻结 → 回收 → 解冻 | Run 3378 保持 `timeout` + 原因保留，实际花费与 11 条结论照常入库 |
| 分组/事故/工具/知识 | 容器栈两次真实分析 | 1848 组 / 593 事故、2000 事件回填归属；`tool_usage` 记录两个工具；候选 4/6 条进 staging |
| 历史事故注入 | 同项目第二次分析 | `historical_incidents=5`、`incidents_reused=6` |
| 知识闭环 | 经审核接口确认一条候选，再发起分析 | `confirmed_knowledge=1`（Run 3571） |
| 峰谷口径 | 真机配置 + 真机函数 | 高峰口径估算 = 计费 × 1.1 安全边际；控制器 `peak_now` 与 `is_peak_time(now)` 一致 |

镜像一致性：每次改完代码都会 `docker compose up -d --build` 重建，
容器内 `app/**` 的 SHA256 与工作区**逐字节一致**（报告里逐次抽查）——
真机跑的就是当前这份代码，不是几小时前的旧镜像。

---

## 3. 十二项真机未达标（**全部已修复**，逐条见第 0 节）

### 3.1 阶段 02-4：EventGroup / Incident 从不落库 ✅ 本轮已修复（第 0 节第 6 条）

- **现象**：跑完的分析在 `event_groups`、`incidents`、`events.group_id` 上全是 0。
- **证据**：全库 177 条 Run、3183 个事件，`event_groups=0`、`incidents=0`、
  `group_id is not null` 0 条；静态图上 `EventGroupRepository.create_group`、
  `IncidentRepository.create_incident` 未被任何入口点调用。
- **根因**：分组与归并只在 `analyze()` 的内存里做（供 Context 用），
  `tasks/analysis._persist_result` 只落 Insight 与 Evidence。
- **最小修复**：在写终态之前把 `result.groups` / `result.incidents` 落库
  （create_group + 回填 `Event.group_id`；create_incident），
  并把 incident id 经 `persist_insights(incident_id_by_title=...)` 回填给 Insight ——
  这个参数**已经留好了，但没有任何调用方传过值**。

### 3.2 阶段 03-4 / 11-3：runbook 在真机上从不显示 ✅ 本轮已修复（第 0 节第 8 条）

- **现象**：真机结论的 `run_metadata` 全是 `null`，页面看不到处置步骤。
- **证据**：`.dsh/runbook_probe.py` 用真机落库的事件重放同一条函数链：
  候选异常 1 条（`ProcessCrash`），`attach_runbooks` 挂上 **0** 条。
  命中词是 `'crash'`（来自进程名 CrashReporterSupportHelper），
  而 `rb_crash_001` 要求 `segfault|segmentation fault|core dumped|panic`、
  `rb_oom_001` 要求 `Out of memory|oom-killer|Killed process` —— 没有一条覆盖 `crash`。
- **根因**：analyzer 的命中词表（8 个，含最泛的 `crash`）与 runbook 的
  `applies.message_pattern` 白名单不对齐。
- **最小修复**：让两侧共用同一份关键词表（runbook 加 `crash`，或 analyzer 在
  message 里带上归一化类别词），并补一条"用真机事件重放"的测试。

### 3.3 阶段 05-1 / 05-2 / 05-3：工具注册表与用量记录没接上 ✅ 本轮已修复（第 0 节第 10 条）

- **现象**：三个工具里只有 `stats_calculator` 在链路上跑过（**直接 import 调用**，
  绕开注册表）；`event_filter`、`time_window` 真机执行 0 次；
  `Run.tool_usage` 恒为 `null`。
- **证据**：函数追踪 `{stats_calculator: 2, event_filter: 0, time_window: 0}`；
  `get_tool_registry` 真机执行 0 次；Run 3023/3024 的 `tool_usage` 均为 `None`。
- **根因**：`app/tools/wiring.py` 装配了注册表，但生产路径没有任何地方取用它；
  `append_tool_usage` 同理（只有集成测试在调）。
- **最小修复（需你定口径）**：
  ① 让管线经 `get_tool_registry().call(...)` 调工具并 `append_tool_usage` 记账；
  ② 或者承认"注册表只是给领域包用的扩展点"，把这条验收改写清楚。
  **两条路都行，但不能像现在这样：验收写着"注册器能按名取用"，真机上没人取用。**

### 3.4 阶段 07-5：月度预算对真实用量永远不触发 ✅ 本轮已修复（见第 0 节第 1、2 条）

- **现象**：本月已花多少都拦不住新 Run。
- **证据**：全库 177 条 Run 里 **91 条真的花过钱**（合计 ¥4.05），
  而 `started_at` 非空的只有 **2 条** —— 那 2 条还是本脚本手工插入的。
  `cost_sum_since` 的 SQL 带 `started_at is not null`，于是真实花费全被排除。
  逻辑本身没问题：手工造出 `started_at` 之后，本月已花满 → **402**，
  上月花满 → **放行**（跨月边界正确）。
- **根因**：没有任何代码写 `AgentRun.started_at` —— `create_queued`、`apply_status`、
  `touch_heartbeat` 都不写它。
- **最小修复**：Worker 把 Run 置 `running` 时写 `started_at`（`apply_status` 里补一行），
  并加一条"跑完一次真机分析后 `started_at` 非空"的测试。

### 3.5 阶段 07-6：峰谷价只在计费侧生效 ✅ 本轮已修复（第 0 节第 9 条）

- **现象**：高峰时段的成本**估算**偏低，可能放行一个注定超预算的 Run。
- **证据**：真机配置 + 真机函数：Pre-check 估算 L3 = ¥0.017600（与峰谷无关），
  同一份 token 在高峰窗口的实际计费 = ¥0.032000（×2.0）→ 低估 1.82 倍。
- **根因**：`estimate_run_cost()` 调 `estimate_cost()` 时没有传 `peak`，
  而 `Router.generate()` 计费时传了 `_is_peak_now()`。**同一份单价，两个口径。**
- **最小修复**：`build_cost_controller` 把 `is_peak_time(now)` 传进估算，
  与计费侧统一。

### 3.6 阶段 08-2：僵尸 Run 无人回收，且"杀了就变 queued" ✅ 本轮已修复（见第 0 节第 4 条）

- **现象**：分析中强杀 Worker，Run **永远**停在 `queued`。
- **证据**：Run 3029 在 11:53 被杀，12:03 复查仍是 `queued`（心跳阈值 300s 早已超过）。
- **根因（两处叠加）**：
  ① `celery_app` 里**没有 `beat_schedule`** —— beat 容器空转，
     `reclaim_zombies` 在生产路径上没有任何调用方；
  ② 即便接上，`find_zombie_runs` 只认 `status='running'`，
     而 Worker 被杀时它的 `running` 写入随事务一起丢了（见 3.7），
     停在 `queued` 的 Run **连扫描条件都不满足**。
     （`_record_crash` 的注释写着"停在 queued 的 Run 由心跳超时回收判定" —— 与实际行为不符。）
- **最小修复**：加 `beat_schedule` 定时跨项目 `reclaim_zombies`；
  并让回收条件覆盖"`queued` 且创建/开始时间超过阈值"；
  或让 Worker 一开始就用短事务把 `running` 提交掉。

### 3.7 阶段 08-4：取消不生效（Worker 的长事务把取消挡住） ✅ 本轮已修复（见第 0 节第 3 条）

- **现象**：分析进行到一半调取消，接口返回 200，Run 仍跑完并计费。
- **证据**：Run 3028 —— `cancel_http=200`、库里 `cancel_requested=true`、
  最终状态 `completed`、花费 ¥0.032416。
  `.dsh/cancel_stale_probe.py` 复现根因：会话 A 写一次心跳不提交（Worker 的常态），
  另一个连接去改 `cancel_requested` → **`LockNotAvailable`（锁等待超时）**；
  会话 A 提交后同样的 UPDATE 立刻成功。
- **根因**：`tasks/analysis._execute` **整个任务只用一个 Session**，
  从第一次 `apply_status(running)` / `heartbeat()` 起就在 `agent_runs` 那一行上持写锁，
  直到任务结束才 commit。API 进程的取消 UPDATE 只能等它提交 —— 那时分析早已结束。
- **最小修复（任一，建议 ①+③）**：
  ① Worker 把"状态与心跳"用短事务提交，分析结果另起一段事务；
  ② 取消写入改到独立连接并带 `statement_timeout`，超时就明确报"取消失败"；
  ③ Worker 的取消检查点用**新会话**读（绕开行锁等待与身份映射）。

### 3.8 阶段 09-4：历史相似事故永远为空 ✅ 本轮已修复（第 0 节第 7 条）

- **现象**："历史事故记忆库"实际上没有内容可用。
- **证据**：`PipelineInput.historical_incidents` 在 `app/` 内**没有任何赋值处**；
  真机 `incidents` 表为 0（见 3.1）。
- **根因**：Worker 构造 `PipelineInput` 时只传了 events，没查历史 Incident。
- **最小修复**：构造输入时按 project + 时间窗/签名取最近 N 条 Incident 传入；
  依赖 3.1 先把 Incident 落库。

---

## 4. 待实测：**已清零**

首轮列了 8 条；02-1（迁移回滚）、08-5（模型不可用降级）、08-6（错误分类）、
11-4（重试入口）在第三批补齐；07-2、09-2、09-6、14-1 在第四批补齐。
**当前表上没有「待实测」项** —— 每一行都有测试证据 + 真机接线 + 真机实测。

需要说明的是这批补充本身的价值：它们不是把 ✅ 凑齐，而是各自都撞出了真缺陷 ——
迁移回滚暴露过判据写错（`alembic_version` 本来就会留下）、模型不可用暴露过
"假正常"的 `completed`、重试入口暴露过"不填时间范围就无法重试"、
假 event_id 与极小预算暴露过"执行器看不到管线内部的降级标记"。
**「待实测」这一列不是待办清单，是"这里可能有洞、只是还没人踩"。**

---

## 5. 顺带发现（不在验收条目内，但会误导读者）

1. **README 写着"四个脚本"，表里已经 10 个** —— 已在本轮修正。
2. `_record_crash` 的注释与 `find_zombie_runs` 的实际条件不符（见 3.6②）——
   已随第一批修复一并订正（注释与行为都指向"停在 queued 的 Run 没有时间基准"这一事实）。
3. `persist_insights(incident_id_by_title=...)` 参数留好了但无人传值 ——
   现已按**证据事件**关联（`incident_by_event`），不再靠标题字符串碰运气；
   标题映射作为兼容路径保留。
4. `CostController.post_check()` / `partial_success_metadata()` 没有被调用 ——
   对应的行为在 `tasks/analysis.py` 里**手写重复实现**了一遍（成本累加、中断元数据）。
   行为正确，但这些方法的存在会让人以为"闸门是它们在做"。**这一条仍未处理**：
   要么接上、要么删掉，属于"读代码会被误导"的存量问题。
5. 验收表原表**漏了两条**（本轮才发现）：阶段 09 的「候选知识写 staging」
   （计划第 751 行）与计划第 22 节第 13 条的知识闭环。已补成 09-8 / 13-5 ——
   表本身也会漏，这就是为什么要拿代码与计划**逐条对**，而不是只维护既有清单。
6. 真机验证在库里留下了一批数据：全库 Run 从 177 条涨到 300+、事件 3000+，
   另有 593+ 条事故与 20+ 条候选知识。它们是**证据**（报告里按 id 点名），
   需要清理时按 `project_id` 删项目即可（所有表都以 Project 为边界）。
7. `time_window` 是唯一**有意保留**没有生产调用方的工具：
   复杂度评估用的是间隔聚类（`count_time_clusters`），与"按固定窗口切分"
   不是一回事，硬接进管线会改变等级判定口径 —— 宁可如实标注，也不为了用而用。

---

## 6. 修复状态

**四批共 12 处真机不生效 + 8 条"待实测"，全部修复/补齐并逐条真机复验**（第 0 节）。
验收表当前是 **60 / 0 / 0**（达标 / 待实测 / 未达标）。

另有第 5 节第 4 条（`post_check` / `partial_success_metadata` 与 `tasks/analysis.py`
里的手写实现重复）属于**存量冗余**，不影响行为，留待下次清理。

---

## 7. 复现

```bash
python .dsh/stage_acceptance.py      # 出表：阶段验收表-真机口径.md
python .dsh/wiring_graph.py --gaps   # 穷举"定义了但入口点走不到"的符号
python .dsh/parser_eval.py           # 真实语料解析率/脱敏误伤率
python .dsh/golden_eval.py           # Golden Set（离线零成本）
node   .dsh/final_verify.cjs         # 容器全栈 16 项 E2E
python .dsh/budget_gate_check.py     # 成本闸门（含月度预算跨月 / 峰谷价）
python .dsh/real_path_evidence.py --help   # 真机证据采集的四种模式见文件头
```
